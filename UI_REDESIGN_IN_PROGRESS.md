# KickPot premium UI redesign

This branch is isolated from `claude/kickpot-pwa-deploy-7ashz5` to avoid clobbering concurrent backend/frontend work.

Scope: visual system and UI presentation only. Do not directly modify `supabase/schema.sql`, `server.mjs`, or `public/app.js` without explicit coordination.
